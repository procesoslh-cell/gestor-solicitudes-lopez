# Backend Python legacy

Esta carpeta queda solo como referencia histórica.

La aplicación actual usa **Node/Express** como backend real:

```bash
cd backend
npm install
npm run dev
```

No dockerizar ni levantar esta carpeta para producción salvo que se decida reescribir el proyecto.
