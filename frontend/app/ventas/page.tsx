'use client';
import{useEffect,useMemo,useState}from'react';import Shell from'../../components/Shell';import{apiGet,apiPost}from'../../lib/api';
export default function Ventas(){const[p,setP]=useState<any[]>([]),[sales,setSales]=useState<any[]>([]),[contacts,setContacts]=useState<any[]>([]),[type,setType]=useState('Presupuesto'),[pid,setPid]=useState(''),[q,setQ]=useState(1),[search,setSearch]=useState(''),[cart,setCart]=useState<any[]>([]),[payment,setPayment]=useState('Efectivo'),[discount,setDiscount]=useState(0),[err,setErr]=useState(''),[customer,setCustomer]=useState<any>({contact_id:'',customer_name:'',customer_phone:'',customer_document:'',customer_email:'',customer_address:'',seller:'',notes:''});const load=async()=>{setP(await apiGet('/products'));setSales(await apiGet('/sales'));setContacts(await apiGet('/contacts'))};useEffect(()=>{load()},[]);const filtered=useMemo(()=>p.filter(x=>`${x.sku} ${x.name} ${x.brand}`.toLowerCase().includes(search.toLowerCase())).slice(0,80),[p,search]);function selectContact(id:string){const c=contacts.find(x=>String(x.id)===String(id));setCustomer({...customer,contact_id:id,customer_name:c?.name||'',customer_phone:c?.phone||'',customer_document:c?.document||'',customer_email:c?.email||'',customer_address:c?.address||''})}function add(productId=pid){const x=p.find(y=>String(y.id)===String(productId));if(!x)return;setCart([...cart,{product_id:x.id,sku:x.sku,name:x.name,quantity:+q,unit_price:x.sale_price,total:+q*x.sale_price}]);setPid('');setQ(1)}function remove(i:number){setCart(cart.filter((_,idx)=>idx!==i))}async function submit(){setErr('');try{await apiPost('/sales',{type,payment_method:payment,discount:+discount,...customer,contact_id:customer.contact_id?+customer.contact_id:null,items:cart.map(c=>({product_id:c.product_id,quantity:c.quantity,unit_price:c.unit_price}))});setCart([]);setDiscount(0);setCustomer({contact_id:'',customer_name:'',customer_phone:'',customer_document:'',customer_email:'',customer_address:'',seller:'',notes:''});load()}catch(e:any){setErr(e.message)}}const subtotal=cart.reduce((a,b)=>a+b.total,0),total=Math.max(subtotal-(+discount||0),0);return <Shell><div className='section-title'><h1>Ventas</h1><div className='tabs'><button className={`tab ${type==='Presupuesto'?'active':''}`} onClick={()=>setType('Presupuesto')}>Presupuesto</button><button className={`tab ${type==='Pedido de venta'?'active':''}`} onClick={()=>setType('Pedido de venta')}>Pedido de venta</button></div></div>{err&&<div className='card' style={{color:'crimson'}}>{err}</div>}<br/><div className='pos-layout'><div className='card'><h3>Cargar cliente</h3><div className='form-row'><select className='input' value={customer.contact_id} onChange={e=>selectContact(e.target.value)}><option value=''>Crear/seleccionar cliente</option>{contacts.filter(x=>x.type==='Cliente'||x.type==='Taller'||x.type==='Distribuidor').map(x=><option key={x.id} value={x.id}>{x.name} · {x.phone}</option>)}</select>{['customer_name','customer_phone','customer_document','customer_email','customer_address','seller'].map(k=><input key={k} className='input' placeholder={k} value={customer[k]||''} onChange={e=>setCustomer({...customer,[k]:e.target.value})}/>)}</div><textarea className='input' placeholder='Observaciones' value={customer.notes||''} onChange={e=>setCustomer({...customer,notes:e.target.value})}/><hr/><h3>Productos</h3><div className='form-row'><input className='input' placeholder='Buscar SKU o descripcion' value={search} onChange={e=>setSearch(e.target.value)}/><select className='input' value={pid} onChange={e=>setPid(e.target.value)}><option value=''>Producto</option>{filtered.map(x=><option key={x.id} value={x.id}>{x.sku} {x.name} Stock {x.available_stock} ${x.sale_price}</option>)}</select><input className='input' type='number' value={q} onChange={e=>setQ(+e.target.value)}/><button className='btn' onClick={()=>add()}>Agregar</button></div><div className='grid' style={{gridTemplateColumns:'repeat(auto-fill,minmax(180px,1fr))'}}>{filtered.slice(0,12).map(x=><button key={x.id} className='card' onClick={()=>add(String(x.id))} style={{textAlign:'left',cursor:'pointer'}}><b>{x.sku}</b><div>{x.name}</div><div className='small'>Stock {x.available_stock} · ${Math.round(x.sale_price||0)}</div></button>)}</div></div><div className='invoice'><h3>{type}</h3><p className='small'>{type==='Presupuesto'?'No descuenta stock.':'Descuenta stock al registrar.'}</p><div className='table-wrap'><table className='table'><tbody>{cart.map((c,i)=><tr key={i}><td><b>{c.sku}</b><div className='small'>{c.name}</div></td><td>{c.quantity}</td><td>${c.unit_price}</td><td>${c.total}</td><td><button className='btn secondary' onClick={()=>remove(i)}>x</button></td></tr>)}</tbody></table></div><br/><div className='form-row'><select className='input' value={payment} onChange={e=>setPayment(e.target.value)}><option>Efectivo</option><option>Transferencia</option><option>Tarjeta</option><option>Cuenta corriente</option></select><input className='input' type='number' placeholder='Descuento' value={discount} onChange={e=>setDiscount(+e.target.value)}/></div><p>Subtotal: ${subtotal}</p><div className='total-box'>Total ${total}</div><br/><button className='btn' style={{width:'100%'}} disabled={!cart.length} onClick={submit}>Registrar {type}</button></div></div><br/><h3>Ultimos comprobantes</h3><div className='table-wrap'><table className='table'><thead><tr><th>ID</th><th>Tipo</th><th>Cliente</th><th>Fecha</th><th>Medio</th><th>Total</th></tr></thead><tbody>{sales.map(s=><tr key={s.id}><td>{s.id}</td><td><span className='pill'>{s.type}</span></td><td>{s.customer_name}</td><td>{String(s.created_at).slice(0,19)}</td><td>{s.payment_method}</td><td>${s.total}</td></tr>)}</tbody></table></div></Shell>}
