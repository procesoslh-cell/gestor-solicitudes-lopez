'use client';import{useEffect,useState}from'react';import Shell from'../../components/Shell';import{apiGet,apiPost}from'../../lib/api';export default function Usuarios(){const[u,setU]=useState<any[]>([]),[f,setF]=useState({username:'',password:'',role:'Sucursal'}),[err,setErr]=useState('');const load=async()=>{try{setU(await apiGet('/users'))}catch{setErr('Solo Admin puede ver usuarios')}};useEffect(()=>{load()},[]);async function submit(e:any){e.preventDefault();await apiPost('/users',f);setF({username:'',password:'',role:'Sucursal'});load()}return <Shell><h1>Usuarios</h1>{err&&<div className='card' style={{color:'crimson'}}>{err}</div>}<br/><div className='card'><form onSubmit={submit}><div className='form-row'><input className='input' placeholder='Usuario' value={f.username} onChange={e=>setF({...f,username:e.target.value})}/><input className='input' type='password' placeholder='Clave' value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><select className='input' value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option>Admin</option><option>Sucursal</option></select></div><button className='btn'>Crear usuario</button></form></div><br/><table className='table'><thead><tr><th>ID</th><th>Usuario</th><th>Rol</th></tr></thead><tbody>{u.map(x=><tr key={x.id}><td>{x.id}</td><td>{x.username}</td><td>{x.role}</td></tr>)}</tbody></table></Shell>}